# The only backend the dashboard talks to. Simulation control calls also go
# through this URL (central-system proxies them to node/'s orchestrator) —
# the dashboard never calls node/ directly.
VITE_CENTRAL_API_BASE_URL=http://localhost:8000
